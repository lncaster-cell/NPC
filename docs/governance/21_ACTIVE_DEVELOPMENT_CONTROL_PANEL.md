# Ambient Life v2 — Active Development Control Panel

Дата: 2026-04-08
Статус: active execution control panel (rewrite track)

---

## 0) Текущий статус

- Legacy-контур `Daily Life v1` архивирован в `archive/daily_life_v1_legacy/scripts/daily_life/`.
- Активный каталог `scripts/daily_life/` очищен под v2 и содержит только стартовые файлы.
- Работа ведётся строго по протоколу `одна функция -> одна проверка -> запись результата`.

Ключевые документы:
1. `docs/runtime/40_DAILY_LIFE_V2_REWRITE_PROGRAM_RU.md`
2. `docs/runtime/41_DAILY_LIFE_V2_DESIGN_BASELINE_RU.md`
3. `docs/runtime/42_DAILY_LIFE_V2_REPOSITORY_RESET_LOG_RU.md`

---

## 1) Что делаем сейчас

### Фаза A — Design Baseline (активная)

Цель: зафиксировать архитектурные контракты до написания runtime-логики.

Definition of Done фазы A:
- [ ] Утверждён минимальный data-contract v2.
- [ ] Утверждён event-pipeline v2 (module/area/npc hooks).
- [ ] Утверждён baseline по производительности (budget/degradation).
- [ ] Для первой функции определён локальный smoke-check.

---

## 2) Правила выполнения задач

1. В одном PR — один микро-шаг.
2. Каждый микро-шаг обязан содержать:
   - контракт функции;
   - минимальную реализацию;
   - проверку;
   - краткий отчёт по факту.
3. Нельзя параллельно вводить resolver/materialization/worker в одном изменении.
4. Документация обновляется в том же PR, что и код.

---

## 3) Активный scope

Разрешено:
- дизайн v2-контуров;
- подготовка репозитория;
- добавление минимальных v2-скриптов и smoke-заготовок;
- обновление навигационных документов под v2.

Запрещено:
- возвращать в активный каталог runtime-код v1;
- расширять scope на legal/trade/travel до стабилизации v2 daily-life ядра;
- крупные merge «всё сразу» без поэтапной валидации.

---

## 4) Операционный backlog (ближайшие шаги)

1. Зафиксировать `v2 data-contract` (документ + checklist).
2. Зафиксировать `v2 event-pipeline` и точки деградации.
3. Ввести первую runtime-функцию (helper) + локальный smoke-test.
4. Вести журнал результатов после каждого шага (без пропусков).

---

## 5) Короткий формат отчёта в каждом PR

- Что изменено (1–3 пункта).
- Чем проверено (конкретные команды/скрипты).
- Фактический результат.
- Следующий микро-шаг.
